apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordsmith.fullname" . }}
  labels:
    {{- include "wordsmith.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordsmith.selectorLabels" . | nindent 4 }}
